# Notebook

এখানে মূল exploratory notebook টা রাখুন — শুধু রেফারেন্সের জন্য।
আসল কোড `banglarag/` প্যাকেজে।

commit করার আগে অবশ্যই output মুছে নিন:

```bash
jupyter nbconvert --clear-output --inplace notebook/*.ipynb
```

output-এ Google Drive এর পাথ আর knowledge base এর বড় অংশ থেকে যায়।
